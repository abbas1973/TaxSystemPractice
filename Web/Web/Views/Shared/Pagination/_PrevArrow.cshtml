@{
    int CurrentPage = (int)(ViewBag.CurrentPage as int?);

    string srch = Context.Request.Query["srch"];

    string url = "javascript:void(0);";
    if (CurrentPage > 1)
    {
        url = Context.Request.Path;
        var index = url.IndexOf("?");
        if (index > 0)
        {
            url = url.Substring(0, index);
        }
        url += "?page=" + (CurrentPage - 1);
        if (!string.IsNullOrEmpty(srch))
        {
            url += "&srch=" + srch;
        }
    }
}


<li class="page-item @(CurrentPage == 1 ? "disabled" : "")">
    <a href="@url" class="page-link" rel="@(CurrentPage == 1 ? 1 : CurrentPage - 1)">
        <i class="fas fa-long-arrow-alt-left margin-5px-right xs-display-none"></i> قبلی
    </a>
</li>



